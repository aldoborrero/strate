server:
  port: 4000
  base_url: https://strate.example.com
database:
  type: sqlite
  sqlite:
    path: /tmp/strate.db
ci:
  provider: github
  github:
    webhook_secret: test-secret
    hostname: github.enterprise.com
    token: github-token
auth:
  jwt_secret: test-secret
